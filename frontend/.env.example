# Copy to .env.local. Values prefixed NEXT_PUBLIC_ are exposed to the browser,
# so never put a secret here.

# Base URL of the StellaLens API (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:5000
